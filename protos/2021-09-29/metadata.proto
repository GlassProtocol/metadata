syntax = "proto3";
package metadata;
option go_package = "/;metadata";

import "video/video_metadata.proto";
import "primatives/signature.proto";


message metadata {
  string version = 1; // version number
  repeated signature signatures = 2;
  oneof data {
    video_metadata video = 3;
  }
}